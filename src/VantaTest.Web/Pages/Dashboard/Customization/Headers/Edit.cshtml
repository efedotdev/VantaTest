@page
@model VantaTest.Web.Pages.Dashboard.Customization.Headers.EditModel
@{
}
@section styles {
    <abp-style src="/pages/dashboard/customization/Headers/edit/index.css" />
}
@section scripts {
    <abp-script src="/pages/dashboard/customization/Headers/edit/index.js" />
}
<div class="container p-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-header bg-warning text-dark fw-bold">
                    <i class="bi bi-pencil-square me-2"></i>Başlık Düzenle
                </div>
                <div class="card-body">
                    <form method="post" enctype="multipart/form-data">

                        <div class="mb-4">
                            <label for="headerImageInput" class="form-label fw-bold">Başlık Görseli</label>

                            <div class="image-upload-container text-center" id="uploadContainer" onclick="document.getElementById('headerImageInput').click()" style="cursor: pointer;">

                                <img id="imagePreviewBox" class="image-preview mb-2" src="@Model.Header.ImagePath" alt="Mevcut Görsel" style="display: block; margin: 0 auto;">

                                <div id="uploadPlaceholder">
                                    <i class="bi bi-arrow-repeat fs-2 text-muted"></i>
                                    <p class="text-muted mb-0 mt-2">Mevcut resmi güncellemek veya değiştirmek için tıklayın</p>
                                </div>
                            </div>

                            <input type="file" name="UploadedImage" class="form-control d-none" id="headerImageInput" accept="image/*">

                            <input type="hidden" asp-for="Header.ImagePath" />
                        </div>
                        <div class="mb-3">
                            <label asp-for="Header.MainHeader" class="form-label fw-bold">Başlık</label>
                            <input type="text" asp-for="Header.MainHeader" class="form-control" required>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Header.Description" class="form-label fw-bold">Açıklama</label>
                            <textarea asp-for="Header.Description" class="form-control" rows="3" required></textarea>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a asp-page="./Index" class="btn btn-secondary">İptal</a>
                            <button type="submit" class="btn btn-warning">Güncelle</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>